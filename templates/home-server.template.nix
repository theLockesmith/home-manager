{ config, pkgs, ... }:

{
    home.file = {
        ## Server files
    };

    home.packages = with pkgs; [
        ## Server packages
    ];

    home.sessionVariables = {
        ## Sever variables
    };
}